# Polish

## General
- We need a preloader on the code editor if loading a stored / persisted ZIP please, it takes a while.

## Package management
- The terminal still says `npm install @perry-rylance/midi @perry-rylance/midi-macros`. It should just `npm install` and the default / loaded / persisted `package.json` should be used.
- Remove the `#status` from the package management tab, it's not helpful.
- Remove the Installed packages header
- Add a list item at the end to add packges, this should open a modal.
- Move the package name(s) and install button into the new modal.
